name: aclub2
description: Main Flutter frontend.
version: 1.0.0+1
publish_to: none
environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  firebase_core: ^4.14.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
