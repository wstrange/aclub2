/// Shared domain models for the aclub application.
///
/// Pure Dart — safe to import from CLI and server packages that do not run
/// Flutter. For the Firestore ODM runtime (queries, `appSchema`) import
/// `package:shared_models/odm.dart` instead.
library;

export 'models.dart';
export 'firestore_converter.dart';
