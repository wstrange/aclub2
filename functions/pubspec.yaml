name: aclub_functions
description: Cloud Functions for aclub, running in the Firebase Emulator.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  firebase_functions: ^0.6.0
  firebase_admin_sdk: ^0.5.0

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^6.0.0